/**
 * Test values for the secrets this harness adds to `secrets.required` — none.
 * `vitest.config.ts` seeds these alongside the ones every leader has.
 */
export const HARNESS_TEST_ENV: Record<string, string> = {};
