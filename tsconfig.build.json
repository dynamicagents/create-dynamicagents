{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    // No maps: they point at `src/`, which is not published.
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.spec.ts"]
}
